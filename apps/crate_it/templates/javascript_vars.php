<!-- workaround to make var avalaide to javascript -->
<div id="hidden_vars" hidden="hidden">
    <span id="description_length">{{ description_length }}</span>
    <span id="max_sword_mb">{{ max_sword_mb }}</span>
    <span id="max_zip_mb">{{ max_zip_mb }}</span>
    <span id="selected_crate">{{ selected_crate }}</span>
</div>
